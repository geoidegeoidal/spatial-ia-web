# AGENTS.md

## Project Overview
`spatial-ia-web` is a modern, high-aesthetic landing page for the Spatial / GIS & AI Bootcamp ("Desarrollo de Apps Territoriales con IA").

## Core Architecture
- Approved **Hydra-inspired** direction: black (`#080909`), orange (`#ff541c`), Geist Pixel + Manrope + Space Mono, reactive particle terrain → sphere, technical borders and scroll transitions. Production entry is `index.html`, styled by `assets/site.css`; shared motion lives in `assets/motion.js` / `assets/motion.css`.
- Zero server backend architecture with pure client-side interactivity and Google Apps Script CRM integration.

## OpenSpec Specification Framework (Mandatory)
All architectural features, business rules, and changes in this repository are managed through the **OpenSpec** framework in `openspec/`:
- **Living Specifications (`openspec/specs/`):** Source of Truth for system capabilities (`landing-page`, `crm-automation`, `diploma-engine`, `deployment-ops`).
- **Traceability Matrix (`openspec/specs/traceability-matrix.md`):** Bidirectional map between Rule IDs (`RULE-*`), source code files/functions, and verification methods.
- **Change Lifecycle (`openspec/changes/` & `openspec/archive/`):** Any non-trivial modification must follow the **Propose → Apply → Verify → Archive** lifecycle using Delta Specs.

## Key Design & Safety Rules
- **Design Tokens (`RULE-UI-001`)**: Background `#080909`, cards `#111111`, primary accent `#ff541c`, Geist Pixel (local family `Pixel`) + Manrope + Space Mono. User selected animated Hydra for the official page on 2026-09-18; this supersedes the prior Superdesign hands/atmosphere.
- **Brand favicon:** `assets/favicon.svg` uses the Hydra black/orange palette and a pixel `S/` mark; keep the linked SVG lightweight and legible at 16 px.
- **Form Status (`RULE-UI-004`)**: V4 registrations are closed. The V5 landing contains the real form at `#registro` / `#protocolo`, connected to the separately verified CRM endpoint. It requires JSON `result === "success"`, blocks duplicate submits and keeps errors accessible. User authorized deployment on 2026-09-18; commit `7dfc4c9` is published on `master`/`gh-pages`, Pages run `35409668450` succeeded, and the public form was verified without submitting data.
- **V5 preparation:** `index.html` is the V5 landing; `preview_v5.html` redirects to it. Hydra was published on 2026-09-18 after the explicit request “publicalo”: `e4c423c` pushed to master/gh-pages, Pages run `35364535749` succeeded and the live page was verified. Registration was reopened after the later explicit request “deploy”: `7dfc4c9`, Pages run `35409668450`. All original content is preserved. Pending V4 closure requirements live in `openspec/changes/prepare-v5-and-close-v4/`. V4 `Accesos Enviados` does not itself prove course completion for certification.
- **V5 CRM automation (`RULE-CRM-008`)**: V5 sheet is `13MgX1IAFHdkJCMrv6Iz1uu1q4zy8fZWR9HICcZ2glDs`, existing empty `gid=0` with A:H; never regenerate/reformat it. Full replacement is `crm_v5_script.gs`, guide `CRM_V5.md`; keep its Spanish section comments because the user relies on them when copy/pasting into Apps Script. `prepararCRMV5` validates the existing sheet/timezone and replaces only the hourly `ejecutarCRMV5` trigger. Every valid submission automatically validates, neutralizes formula/control injection, writes, notifies admin and sends participant payment email. The hourly CRM sends 24/72h reminders. Operator validates payment by writing exact `Pagado`; next cycle automatically sends the updated V5 tutorial and sets `Tutorial Enviado`. No operator confirmation in normal email paths; only V4 diploma dispatch uses visual confirmation. Frontend must parse JSON and require `result === "success"` because Apps Script application errors may still use HTTP 200. Email-keyed transitions tolerate sorting/preserve concurrent `Pagado`; uncertain failures get non-retry review states. Mock test: `test_crm_v5.cjs`. Endpoint: `https://script.google.com/macros/s/AKfycbzkX21TwO9LqpJao8mjVZ2Guc98J-yvTw-5B1cKP38gzwNxoNQIm0jImAKVoTIi8Iw3rQ/exec`. Authorized real POST created row 2 `Pendiente`; user confirmed both email receipts. Real `Pagado` → `Tutorial Enviado` and tutorial receipt also confirmed. Frontend integration and public reopening are complete.
- **V5 typography and offer:** Upright typography; Geist Pixel / Manrope / Space Mono. Dates: October 16–18, 20:00–21:30, 2026, America/Santiago (UTC−3), 4.5 live hours. Chile: CLP 35,000 general / 30,000 students. Student pricing is valid only for Chile: state it on the price card and FAQ, with no USD student equivalent. International general is USD 36, verified read-only in the user-confirmed PayPal https://www.paypal.com/ncp/payment/2PVCP7EQT3DWU on 2026-09-18. **V5 has no coupons or promotional codes; do not migrate CONMAPAS coupon logic from V4.** ConMapas may appear only as the instructor's company/experience. Registration is open in production. V4 pricing/certification below is historical, not V5 checkout pricing.
- **V5 copy and layout:** Use neutral Spanish suitable for Chile, without Argentine voseo (`imagina`, `crea`, `conoce`, `selecciona`). Keep syllabus cards aligned, not vertically staggered. Instructor bio was checked against the live LinkedIn profile through the user's signed-in Edge window using Windows UI Automation (direct HTTP returns 999). Profile supports UAH geography degree, 8+ years, ConMapas, participation in the Servel georeferenced roll team and AutoAtlas Pro. User clarified both current roles coexist: employment a contrata at Ministerio del Medio Ambiente, and participation a honorarios in a project at Ministerio de las Culturas, las Artes y el Patrimonio. Do not describe team participation as sole leadership.
- **UX baseline:** Evaluate UI changes against all heuristics catalogued at https://lawsofux.com/es/, especially cognitive load, Hick, Fitts, Jakob, proximity, common region, similarity, selective attention, aesthetic-usability and peak-end. Treat them as design constraints alongside accessibility, not as decoration.
- **Instructor portfolio and photo:** https://geoidegeoidal.github.io/ — linked alongside LinkedIn in V5. User explicitly requested the photo and expanded profile: `assets/instructor.jpg` in natural square proportions and color, five original paragraphs retained plus three supported paragraphs on geography/data/tools and teaching approach. Preserve both current ministry roles and team participation wording.
- **Analytics:** User selected hosted GoatCounter (no own server). `index.html` loads `https://gc.zgo.at/count.js` asynchronously with endpoint `https://julloar.goatcounter.com/count`; preview redirect has no tracker to avoid double counting. V5 native click events: `v5-interes-registro`, `v5-programa`, `v5-portafolio`, `v5-linkedin`; interest is not a completed registration. Browser integration verified with intercepted requests. Consult latest HANDOFF for deployment/real-receipt evidence. Dashboard: https://julloar.goatcounter.com/.
- **V5 syllabus:** Latest 50-page presentation supersedes the 53-slide version: S01 agents/SDD/OpenSpec (Explore, Propose, Apply; proposal.md/specs/design.md/tasks.md), KISS/HTML/Tailwind/Vanilla JS; S02 GeoJSON/EDA/MapLibre, filters/popups/UI audits; S03 Turf buffers/dissolve, filtered exports, shared symbology/legend source, Chart.js shared filter state, responsive UI, GitHub CLI/Pages. Do not name practice layers or datasets. Keep 20:00 start and 4.5 live hours; avoid guarantees of full advanced implementation in that time.
- **V5 rendering:** Previous Superdesign blur/blending caused software Edge slowdowns. Current Hydra uses bounded Canvas work, no blur/blends, pause/reduced-motion, hidden/offscreen suspension and native scroll. Official-page benchmark: 331 intervals over 5.5 s, median/p95 16.7 ms, max 16.8 ms, no >50 ms frames or long tasks. Use `overflow:clip` on the hero: `hidden` allowed focus/resize to retain internal horizontal scroll and clip text. Reveal content immediately on keyboard focus.
- **Slides usage:** User explicitly wants only the updated syllabus derived from the presentation. Do not publish, embed, or link the supplied PDF or slide images on the landing page.
- **Alternative redesign samples (2026-09-18):** `proposals/01-hydra.html` and `proposals/02-illoca.html` remain comparison previews without analytics/forms/payments. Hydra was selected, promoted to `index.html` and published; Illoca's alternate tokens remain sample-scoped. Shared runtime moved to `assets/`. Open directly in browser; `node test_redesign.js` for sample content/link checks. Initial work archived under `2026-09-18-two-visual-redesigns`; promotion under `2026-09-18-promote-hydra-official`.
- **Reference motion is essential:** User rejected static interpretations of HydraDB/Illoca. Review loading, cursor and scroll sequences, not just screenshots. Runtime `assets/motion.js` / `assets/motion.css` serves official Hydra and both samples. Preserve particle morph, original Illoca camera/buildings/panel, pause, reduced-motion, offscreen/hidden suspension and SVG fallback. Browser tests `test_redesign_motion.cjs` and `test_site_hydra.cjs` require existing Playwright + Edge (`NODE_PATH=%TEMP%/opencode/node_modules`). Demo videos `proposals/*-motion.webm`.
- **No omitted sections:** `test_site_hydra.cjs` checks seven content hashes: five original pre-migration blocks plus offer/FAQ updated for the authorized V5 country/pricing change (student Chile only / international general USD 36). Preserve full program (12 topics, 3 outputs, 3 details, 10 technologies), original profile, facts/notice, four FAQs and footer. New profile content is excluded from its old hash. Verify four real GoatCounter events with requests intercepted. `SPATIAL_CAPTURE_DIR` optionally saves screenshots to an existing directory.
- **Payment & Pricing Matrix (`RULE-CRM-002`)**:
  - **Chile General:** $30.000 CLP via MercadoPago (`link-id=f7b0764f-2801-4b26-a858-59c416eebe42`) or Bank Transfer.
  - **Chile Estudiante:** $25.000 CLP via MercadoPago (`https://mpago.la/1EvJQi3`) or Bank Transfer.
  - **Internacional General:** 35 USD via PayPal (`https://www.paypal.com/ncp/payment/2PVCP7EQT3DWU`).
  - **Internacional Estudiante:** 29 USD via PayPal (`https://www.paypal.com/ncp/payment/2PVCP7EQT3DWU`).
- **One-Day Recovery Offer (`RULE-CRM-006`)**: Manual send only for `Recordatorio Final Enviado`; Chile $20.000 CLP via MercadoPago (`https://mpago.la/1E75xtF`), international 22 USD via PayPal (`https://www.paypal.com/ncp/payment/RGT8AG7R7U4DA`), expiring at 23:59 Chile on the send date and transitioning to `Oferta Exclusiva Enviada`.
- **Historical V4 Coupon 'CONMAPAS' (`RULE-CRM-007`)**: Legacy `crm_script.gs` still recognizes the former V4 coupon so historical reminders/rows remain stable. This is V4-only: V5 must not expose or process any coupon.
- **CRM Email Safety**: Never test or authorize a bulk-send function against the production spreadsheet. Use local mocks or an isolated test spreadsheet, and review recipients before any production execution.
- **V4 closure reminder:** User requested re-sharing the recordings folder as a reminder in the diploma email. Include an explicit reminder and prominent button using `LINK_GRABACIONES_DRIVE`, plus the same link in plain text; use the existing closure email, not a separate automatic mailing.
- **Country Selector (`RULE-UI-005`)**: Uses Iconify `circle-flags:*` SVG icons in a custom dropdown with keyboard/mouse event listeners to avoid missing flag emojis on Windows OS. Arrow/Escape navigation and selection return focus to the trigger; changing country synchronizes price and Chile-only student availability.
- **Deployment Branch Target (`RULE-OPS-001`)**: Production site on GitHub Pages is served from the `gh-pages` branch. Any updates pushed to `master` must also be merged and pushed to `gh-pages` (`git checkout gh-pages; git merge master; git push origin gh-pages; git checkout master`) to reflect on the live site.
- **CRM Automation & Diploma Generation (`RULE-DIP-001` - `RULE-DIP-004`)**: V4 diplomas retain 9 hours (5 lectivas + 4 prácticas), Ember Orange HTML → PDF via Apps Script Blob and GmailApp with Canva/Drive/LinkedIn. User explicitly selected recipients in `Carpeta Grabaciones Enviada`; do not use `Accesos Enviados`. User wants copy/paste only: full replacement `crm_script.gs`, guide `CIERRE_V4.md`. No manual spreadsheet IDs/tab names or enable flag: detect the unique B/C/H-compatible tab in the bound spreadsheet, fail closed if ambiguous/unbound. `enviarDiplomasYCierre` shows the exact batch for native Sheets confirmation, then acquires the lock and revalidates; dialogs suspend execution, so never hold the lock across them. Preview is read-only; test sends only to EMAIL_ADMIN without Sheets. Batch is quota-limited and capped at 20; never send newly added, unreviewed recipients. `Enviando Diploma`/`Revisar Diploma` are never auto-retried; verify Gmail Sent manually. Date defaults to Santiago; escape names/date. `test_diplomas_v4.cjs` uses mocks only; Python tests are historical V3. Google PDF conversion and actual sending remain for the operator; no live email has been sent by the agent.
- **Powershell Compatibility (`RULE-OPS-003`)**: Do not use `&&` in shell commands under Windows PowerShell; use `;` or execute sequentially.
